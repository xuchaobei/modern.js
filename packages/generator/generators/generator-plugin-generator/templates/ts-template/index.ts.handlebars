import { PluginContextMethod, ForgedAPI } from '@modern-js/generator-plugin';

export default function (ctx: PluginContextMethod) {
  ctx.setSolutionInfo({
    key: '{{ solutionKey }}',
    name: '{{ solutionName }}',
    base: '{{ baseSolution }}',
  });
  ctx.setApiVersion('1.0.0');
  ctx.onForged((_api: ForgedAPI, _inputData: Record<string, unknown>) => {
    // TODO
  });
}
